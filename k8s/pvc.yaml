apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trivia-db-pvc
  namespace: trivia
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
